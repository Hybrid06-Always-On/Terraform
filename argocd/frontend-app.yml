apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: frontend
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/Hybrid06-Always-On/helm_chart.git
    targetRevision: main
    path: charts/frontend
    helm:
      valueFiles:
        - values-aws.yaml
  ignoreDifferences:
    - group: autoscaling
      kind: HorizontalPodAutoscaler
      name: frontend-hpa
      jqPathExpressions:
        - .spec.minReplicas
  destination:
    server: https://kubernetes.default.svc
    namespace: app
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true